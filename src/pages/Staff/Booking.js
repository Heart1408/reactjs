import React from "react";
import BookingList from "../../components/BookingList";

const Booking = () => {
  return <BookingList onShowConfirmCustomerModel={false} confirmCus={false}/>;
};

export default Booking;
